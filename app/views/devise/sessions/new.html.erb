<div class="app-container">
  <main class="content">
  <%= render 'nav/navblock' %>
  <div class="login-page">
    <div class="login__form-outline">
      <h2 class="heading">Log in</h2>
      <div class="login-form">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="login__field">
          <%= f.label :login %><br />
          <%= f.text_field :login, autofocus: true, class:"field__input"%>
        </div>

          <div class="login__field">
            <%= f.label :password %><br />
            <%= f.password_field :password, autocomplete: "current-password", class:"field__input" %>
          </div>

          <% if devise_mapping.rememberable? %>
            <div class="login__field-checkbox">
              <%= f.check_box :remember_me, class: "field__checkbox" %>
              <%= f.label :remember_me, class: "field__checkbox-label" %>
            </div>
          <% end %>

          <div class="actions">
            <%= f.submit "Log in", class: "login__field-button" %>
          </div>
        <% end %>
        <div class="login__links-container">
          <%= render "devise/shared/links" %>
        </div>
      </div>

    </div>
  </div>

  <%= render 'footer/label' %>
</main>
</div>
